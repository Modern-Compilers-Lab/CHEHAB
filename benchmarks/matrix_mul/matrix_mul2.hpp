#include "seal/seal.h"
#include <cstdint>
#include <unordered_map>
#include <vector>
seal::SEALContext create_context()
{
  seal::EncryptionParameters params(seal::scheme_type::bfv);
  params.set_poly_modulus_degree(8192);
  params.set_plain_modulus(seal::PlainModulus::Batching(8192, 17));
  params.set_coeff_modulus(seal::CoeffModulus::Create(8192, {60, 60, 60}));
  seal::SEALContext context(params, true, seal::sec_level_type::tc128);
  return context;
}

void matrix_mul2(
  std::unordered_map<std::string, seal::Ciphertext> &encrypted_inputs,
  std::unordered_map<std::string, seal::Plaintext> &encoded_inputs,
  std::unordered_map<std::string, seal::Ciphertext> &encrypted_outputs,
  std::unordered_map<std::string, seal::Plaintext> &encoded_outputs, const seal::SEALContext &context,
  const seal::RelinKeys &relin_keys, const seal::GaloisKeys &galois_keys, const seal::PublicKey &public_key)
{
  seal::BatchEncoder encoder(context);
  std::vector<std::int64_t> plaintext19_clear = {1, 0, 0, 0};
  seal::Plaintext plaintext19;
  encoder.encode(plaintext19_clear, plaintext19);
  std::vector<std::int64_t> plaintext7_clear = {9, 2, 0, 18};
  seal::Plaintext plaintext7;
  encoder.encode(plaintext7_clear, plaintext7);
  seal::Encryptor encryptor(context, public_key);
  seal::Ciphertext ciphertext22;
  encryptor.encrypt(plaintext7, ciphertext22);
  seal::Ciphertext ciphertext23 = ciphertext22;
  std::vector<std::int64_t> plaintext3_clear = {5, 3, 2, 0};
  seal::Plaintext plaintext3;
  encoder.encode(plaintext3_clear, plaintext3);
  seal::Ciphertext ciphertext9;
  encryptor.encrypt(plaintext3, ciphertext9);
  seal::Ciphertext ciphertext10 = ciphertext9;
  seal::Ciphertext ciphertext16 = ciphertext10;
  seal::Evaluator evaluator(context);
  seal::Ciphertext ciphertext157;
  evaluator.multiply(ciphertext16, ciphertext23, ciphertext157);
  seal::Ciphertext ciphertext158 = ciphertext157;
  seal::Ciphertext ciphertext160;
  evaluator.rotate_vector(ciphertext158, 1, galois_keys, ciphertext160);
  seal::Ciphertext ciphertext162;
  evaluator.rotate_vector(ciphertext160, 2, galois_keys, ciphertext162);
  seal::Ciphertext ciphertext159 = ciphertext158;
  evaluator.add_inplace(ciphertext159, ciphertext160);
  evaluator.add_inplace(ciphertext159, ciphertext162);
  evaluator.multiply_plain_inplace(ciphertext159, plaintext19);
  seal::Ciphertext ciphertext166 = ciphertext159;
  std::vector<std::int64_t> plaintext6_clear = {1, -10, 9, 2};
  seal::Plaintext plaintext6;
  encoder.encode(plaintext6_clear, plaintext6);
  seal::Ciphertext ciphertext19;
  encryptor.encrypt(plaintext6, ciphertext19);
  seal::Ciphertext ciphertext20 = ciphertext19;
  seal::Ciphertext ciphertext25 = ciphertext20;
  seal::Ciphertext ciphertext146;
  evaluator.multiply(ciphertext16, ciphertext25, ciphertext146);
  seal::Ciphertext ciphertext147 = ciphertext146;
  seal::Ciphertext ciphertext149;
  evaluator.rotate_vector(ciphertext147, 1, galois_keys, ciphertext149);
  seal::Ciphertext ciphertext151;
  evaluator.rotate_vector(ciphertext149, 2, galois_keys, ciphertext151);
  seal::Ciphertext ciphertext148 = ciphertext147;
  evaluator.add_inplace(ciphertext148, ciphertext149);
  evaluator.add_inplace(ciphertext148, ciphertext151);
  evaluator.multiply_plain_inplace(ciphertext148, plaintext19);
  seal::Ciphertext ciphertext155 = ciphertext148;
  seal::Ciphertext ciphertext168 = ciphertext155;
  std::vector<std::int64_t> plaintext5_clear = {0, -7, 1, -8};
  seal::Plaintext plaintext5;
  encoder.encode(plaintext5_clear, plaintext5);
  seal::Ciphertext ciphertext17;
  encryptor.encrypt(plaintext5, ciphertext17);
  seal::Ciphertext ciphertext18 = ciphertext17;
  seal::Ciphertext ciphertext21 = ciphertext18;
  seal::Ciphertext ciphertext24 = ciphertext21;
  evaluator.multiply_inplace(ciphertext16, ciphertext24);
  seal::Ciphertext ciphertext138 = ciphertext16;
  seal::Ciphertext ciphertext140;
  evaluator.rotate_vector(ciphertext138, 1, galois_keys, ciphertext140);
  seal::Ciphertext ciphertext142;
  evaluator.rotate_vector(ciphertext140, 2, galois_keys, ciphertext142);
  seal::Ciphertext ciphertext139 = ciphertext138;
  evaluator.add_inplace(ciphertext139, ciphertext140);
  evaluator.add_inplace(ciphertext139, ciphertext142);
  evaluator.multiply_plain_inplace(ciphertext139, plaintext19);
  seal::Ciphertext ciphertext145 = ciphertext139;
  seal::Ciphertext ciphertext156 = ciphertext145;
  seal::Ciphertext ciphertext167 = ciphertext156;
  seal::Ciphertext ciphertext169 = ciphertext167;
  evaluator.add_inplace(ciphertext169, ciphertext168);
  evaluator.add_inplace(ciphertext169, ciphertext166);
  seal::Ciphertext output3 = ciphertext169;
  std::vector<std::int64_t> plaintext2_clear = {1, 0, 1, -3};
  seal::Plaintext plaintext2;
  encoder.encode(plaintext2_clear, plaintext2);
  seal::Ciphertext ciphertext5;
  encryptor.encrypt(plaintext2, ciphertext5);
  seal::Ciphertext ciphertext6 = ciphertext5;
  seal::Ciphertext ciphertext15 = ciphertext6;
  seal::Ciphertext ciphertext120;
  evaluator.multiply(ciphertext15, ciphertext23, ciphertext120);
  seal::Ciphertext ciphertext121 = ciphertext120;
  seal::Ciphertext ciphertext123;
  evaluator.rotate_vector(ciphertext121, 1, galois_keys, ciphertext123);
  seal::Ciphertext ciphertext125;
  evaluator.rotate_vector(ciphertext123, 2, galois_keys, ciphertext125);
  seal::Ciphertext ciphertext122 = ciphertext121;
  evaluator.add_inplace(ciphertext122, ciphertext123);
  evaluator.add_inplace(ciphertext122, ciphertext125);
  evaluator.multiply_plain_inplace(ciphertext122, plaintext19);
  seal::Ciphertext ciphertext129 = ciphertext122;
  seal::Ciphertext ciphertext109;
  evaluator.multiply(ciphertext15, ciphertext25, ciphertext109);
  seal::Ciphertext ciphertext110 = ciphertext109;
  seal::Ciphertext ciphertext112;
  evaluator.rotate_vector(ciphertext110, 1, galois_keys, ciphertext112);
  seal::Ciphertext ciphertext114;
  evaluator.rotate_vector(ciphertext112, 2, galois_keys, ciphertext114);
  seal::Ciphertext ciphertext111 = ciphertext110;
  evaluator.add_inplace(ciphertext111, ciphertext112);
  evaluator.add_inplace(ciphertext111, ciphertext114);
  evaluator.multiply_plain_inplace(ciphertext111, plaintext19);
  seal::Ciphertext ciphertext118 = ciphertext111;
  seal::Ciphertext ciphertext131 = ciphertext118;
  evaluator.multiply_inplace(ciphertext15, ciphertext24);
  seal::Ciphertext ciphertext101 = ciphertext15;
  seal::Ciphertext ciphertext103;
  evaluator.rotate_vector(ciphertext101, 1, galois_keys, ciphertext103);
  seal::Ciphertext ciphertext105;
  evaluator.rotate_vector(ciphertext103, 2, galois_keys, ciphertext105);
  seal::Ciphertext ciphertext102 = ciphertext101;
  evaluator.add_inplace(ciphertext102, ciphertext103);
  evaluator.add_inplace(ciphertext102, ciphertext105);
  evaluator.multiply_plain_inplace(ciphertext102, plaintext19);
  seal::Ciphertext ciphertext108 = ciphertext102;
  seal::Ciphertext ciphertext119 = ciphertext108;
  seal::Ciphertext ciphertext130 = ciphertext119;
  seal::Ciphertext ciphertext132 = ciphertext130;
  evaluator.add_inplace(ciphertext132, ciphertext131);
  evaluator.add_inplace(ciphertext132, ciphertext129);
  seal::Ciphertext output2 = ciphertext132;
  std::vector<std::int64_t> plaintext1_clear = {-5, 3, 2, 0};
  seal::Plaintext plaintext1;
  encoder.encode(plaintext1_clear, plaintext1);
  seal::Ciphertext ciphertext2;
  encryptor.encrypt(plaintext1, ciphertext2);
  seal::Ciphertext ciphertext3 = ciphertext2;
  seal::Ciphertext ciphertext8 = ciphertext3;
  seal::Ciphertext ciphertext14 = ciphertext8;
  seal::Ciphertext ciphertext83;
  evaluator.multiply(ciphertext14, ciphertext23, ciphertext83);
  seal::Ciphertext ciphertext84 = ciphertext83;
  seal::Ciphertext ciphertext86;
  evaluator.rotate_vector(ciphertext84, 1, galois_keys, ciphertext86);
  seal::Ciphertext ciphertext88;
  evaluator.rotate_vector(ciphertext86, 2, galois_keys, ciphertext88);
  seal::Ciphertext ciphertext85 = ciphertext84;
  evaluator.add_inplace(ciphertext85, ciphertext86);
  evaluator.add_inplace(ciphertext85, ciphertext88);
  evaluator.multiply_plain_inplace(ciphertext85, plaintext19);
  seal::Ciphertext ciphertext92 = ciphertext85;
  seal::Ciphertext ciphertext72;
  evaluator.multiply(ciphertext14, ciphertext25, ciphertext72);
  seal::Ciphertext ciphertext73 = ciphertext72;
  seal::Ciphertext ciphertext75;
  evaluator.rotate_vector(ciphertext73, 1, galois_keys, ciphertext75);
  seal::Ciphertext ciphertext77;
  evaluator.rotate_vector(ciphertext75, 2, galois_keys, ciphertext77);
  seal::Ciphertext ciphertext74 = ciphertext73;
  evaluator.add_inplace(ciphertext74, ciphertext75);
  evaluator.add_inplace(ciphertext74, ciphertext77);
  evaluator.multiply_plain_inplace(ciphertext74, plaintext19);
  seal::Ciphertext ciphertext81 = ciphertext74;
  seal::Ciphertext ciphertext94 = ciphertext81;
  evaluator.multiply_inplace(ciphertext14, ciphertext24);
  seal::Ciphertext ciphertext64 = ciphertext14;
  seal::Ciphertext ciphertext66;
  evaluator.rotate_vector(ciphertext64, 1, galois_keys, ciphertext66);
  seal::Ciphertext ciphertext68;
  evaluator.rotate_vector(ciphertext66, 2, galois_keys, ciphertext68);
  seal::Ciphertext ciphertext65 = ciphertext64;
  evaluator.add_inplace(ciphertext65, ciphertext66);
  evaluator.add_inplace(ciphertext65, ciphertext68);
  evaluator.multiply_plain_inplace(ciphertext65, plaintext19);
  seal::Ciphertext ciphertext71 = ciphertext65;
  seal::Ciphertext ciphertext82 = ciphertext71;
  seal::Ciphertext ciphertext93 = ciphertext82;
  seal::Ciphertext ciphertext95 = ciphertext93;
  evaluator.add_inplace(ciphertext95, ciphertext94);
  evaluator.add_inplace(ciphertext95, ciphertext92);
  seal::Ciphertext output1 = ciphertext95;
  seal::Ciphertext ciphertext194;
  evaluator.multiply(ciphertext23, ciphertext10, ciphertext194);
  seal::Ciphertext ciphertext195 = ciphertext194;
  seal::Ciphertext ciphertext197;
  evaluator.rotate_vector(ciphertext195, 1, galois_keys, ciphertext197);
  seal::Ciphertext ciphertext199;
  evaluator.rotate_vector(ciphertext197, 2, galois_keys, ciphertext199);
  seal::Ciphertext ciphertext196 = ciphertext195;
  evaluator.add_inplace(ciphertext196, ciphertext197);
  evaluator.add_inplace(ciphertext196, ciphertext199);
  evaluator.multiply_plain_inplace(ciphertext196, plaintext19);
  seal::Ciphertext ciphertext203 = ciphertext196;
  seal::Ciphertext ciphertext183;
  evaluator.multiply(ciphertext25, ciphertext10, ciphertext183);
  seal::Ciphertext ciphertext184 = ciphertext183;
  seal::Ciphertext ciphertext186;
  evaluator.rotate_vector(ciphertext184, 1, galois_keys, ciphertext186);
  seal::Ciphertext ciphertext188;
  evaluator.rotate_vector(ciphertext186, 2, galois_keys, ciphertext188);
  seal::Ciphertext ciphertext185 = ciphertext184;
  evaluator.add_inplace(ciphertext185, ciphertext186);
  evaluator.add_inplace(ciphertext185, ciphertext188);
  evaluator.multiply_plain_inplace(ciphertext185, plaintext19);
  seal::Ciphertext ciphertext192 = ciphertext185;
  seal::Ciphertext ciphertext205 = ciphertext192;
  seal::Ciphertext ciphertext174;
  evaluator.multiply(ciphertext10, ciphertext24, ciphertext174);
  seal::Ciphertext ciphertext175 = ciphertext174;
  seal::Ciphertext ciphertext177;
  evaluator.rotate_vector(ciphertext175, 1, galois_keys, ciphertext177);
  seal::Ciphertext ciphertext179;
  evaluator.rotate_vector(ciphertext177, 2, galois_keys, ciphertext179);
  seal::Ciphertext ciphertext176 = ciphertext175;
  evaluator.add_inplace(ciphertext176, ciphertext177);
  evaluator.add_inplace(ciphertext176, ciphertext179);
  evaluator.multiply_plain_inplace(ciphertext176, plaintext19);
  seal::Ciphertext ciphertext182 = ciphertext176;
  seal::Ciphertext ciphertext193 = ciphertext182;
  seal::Ciphertext ciphertext204 = ciphertext193;
  seal::Ciphertext ciphertext206 = ciphertext204;
  evaluator.add_inplace(ciphertext206, ciphertext205);
  evaluator.add_inplace(ciphertext206, ciphertext203);
  seal::Ciphertext output4 = ciphertext206;
  std::vector<std::int64_t> plaintext0_clear = {1, 2, 3, -2};
  seal::Plaintext plaintext0;
  encoder.encode(plaintext0_clear, plaintext0);
  seal::Ciphertext ciphertext0;
  encryptor.encrypt(plaintext0, ciphertext0);
  seal::Ciphertext ciphertext1 = ciphertext0;
  seal::Ciphertext ciphertext4 = ciphertext1;
  seal::Ciphertext ciphertext7 = ciphertext4;
  seal::Ciphertext ciphertext13 = ciphertext7;
  evaluator.multiply_inplace(ciphertext23, ciphertext13);
  seal::Ciphertext ciphertext47 = ciphertext23;
  seal::Ciphertext ciphertext49;
  evaluator.rotate_vector(ciphertext47, 1, galois_keys, ciphertext49);
  seal::Ciphertext ciphertext51;
  evaluator.rotate_vector(ciphertext49, 2, galois_keys, ciphertext51);
  seal::Ciphertext ciphertext48 = ciphertext47;
  evaluator.add_inplace(ciphertext48, ciphertext49);
  evaluator.add_inplace(ciphertext48, ciphertext51);
  evaluator.multiply_plain_inplace(ciphertext48, plaintext19);
  seal::Ciphertext ciphertext55 = ciphertext48;
  evaluator.multiply_inplace(ciphertext25, ciphertext13);
  seal::Ciphertext ciphertext36 = ciphertext25;
  seal::Ciphertext ciphertext38;
  evaluator.rotate_vector(ciphertext36, 1, galois_keys, ciphertext38);
  seal::Ciphertext ciphertext40;
  evaluator.rotate_vector(ciphertext38, 2, galois_keys, ciphertext40);
  seal::Ciphertext ciphertext37 = ciphertext36;
  evaluator.add_inplace(ciphertext37, ciphertext38);
  evaluator.add_inplace(ciphertext37, ciphertext40);
  evaluator.multiply_plain_inplace(ciphertext37, plaintext19);
  seal::Ciphertext ciphertext44 = ciphertext37;
  seal::Ciphertext ciphertext57 = ciphertext44;
  evaluator.multiply_inplace(ciphertext13, ciphertext24);
  seal::Ciphertext ciphertext27 = ciphertext13;
  seal::Ciphertext ciphertext29;
  evaluator.rotate_vector(ciphertext27, 1, galois_keys, ciphertext29);
  seal::Ciphertext ciphertext31;
  evaluator.rotate_vector(ciphertext29, 2, galois_keys, ciphertext31);
  seal::Ciphertext ciphertext28 = ciphertext27;
  evaluator.add_inplace(ciphertext28, ciphertext29);
  evaluator.add_inplace(ciphertext28, ciphertext31);
  evaluator.multiply_plain_inplace(ciphertext28, plaintext19);
  seal::Ciphertext ciphertext34 = ciphertext28;
  seal::Ciphertext ciphertext45 = ciphertext34;
  seal::Ciphertext ciphertext56 = ciphertext45;
  seal::Ciphertext ciphertext58 = ciphertext56;
  evaluator.add_inplace(ciphertext58, ciphertext57);
  evaluator.add_inplace(ciphertext58, ciphertext55);
  seal::Ciphertext output0 = ciphertext58;
  encrypted_outputs.insert({"output3", output3});
  encrypted_outputs.insert({"output2", output2});
  encrypted_outputs.insert({"output1", output1});
  encrypted_outputs.insert({"output4", output4});
  encrypted_outputs.insert({"output0", output0});
}
