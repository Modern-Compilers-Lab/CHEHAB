#include "seal/seal.h"
#include <cstdint>
#include <unordered_map>
#include <vector>
seal::SEALContext create_context()
{
  seal::EncryptionParameters params(seal::scheme_type::bfv);
  params.set_poly_modulus_degree(8192);
  params.set_plain_modulus(seal::PlainModulus::Batching(8192, 17));
  params.set_coeff_modulus(seal::CoeffModulus::Create(8192, {60, 60, 60}));
  seal::SEALContext context(params, true, seal::sec_level_type::tc128);
  return context;
}

void matrix_mul1(
  std::unordered_map<std::string, seal::Ciphertext> &encrypted_inputs,
  std::unordered_map<std::string, seal::Plaintext> &encoded_inputs,
  std::unordered_map<std::string, seal::Ciphertext> &encrypted_outputs,
  std::unordered_map<std::string, seal::Plaintext> &encoded_outputs, const seal::SEALContext &context,
  const seal::RelinKeys &relin_keys, const seal::GaloisKeys &galois_keys, const seal::PublicKey &public_key)
{
  seal::BatchEncoder encoder(context);
  std::vector<std::int64_t> plaintext19_clear = {1, 0, 0, 0};
  seal::Plaintext plaintext19;
  encoder.encode(plaintext19_clear, plaintext19);
  std::vector<std::int64_t> plaintext7_clear = {9, 2, 0, 18};
  seal::Plaintext plaintext7;
  encoder.encode(plaintext7_clear, plaintext7);
  seal::Encryptor encryptor(context, public_key);
  seal::Ciphertext ciphertext22;
  encryptor.encrypt(plaintext7, ciphertext22);
  seal::Ciphertext ciphertext23 = ciphertext22;
  std::vector<std::int64_t> plaintext3_clear = {5, 3, 2, 0};
  seal::Plaintext plaintext3;
  encoder.encode(plaintext3_clear, plaintext3);
  seal::Ciphertext ciphertext9;
  encryptor.encrypt(plaintext3, ciphertext9);
  seal::Ciphertext ciphertext10 = ciphertext9;
  seal::Ciphertext ciphertext16 = ciphertext10;
  seal::Evaluator evaluator(context);
  seal::Ciphertext ciphertext333;
  evaluator.multiply(ciphertext16, ciphertext23, ciphertext333);
  seal::Ciphertext ciphertext334 = ciphertext333;
  seal::Ciphertext ciphertext335 = ciphertext334;
  seal::Ciphertext ciphertext337;
  evaluator.rotate_vector(ciphertext335, 1, galois_keys, ciphertext337);
  seal::Ciphertext ciphertext339 = ciphertext337;
  evaluator.rotate_vector_inplace(ciphertext339, 1, galois_keys);
  seal::Ciphertext ciphertext343 = ciphertext339;
  evaluator.rotate_vector_inplace(ciphertext343, 1, galois_keys);
  seal::Ciphertext ciphertext348 = ciphertext343;
  evaluator.rotate_vector_inplace(ciphertext348, 1, galois_keys);
  seal::Ciphertext ciphertext336 = ciphertext335;
  evaluator.add_inplace(ciphertext336, ciphertext337);
  evaluator.add_inplace(ciphertext336, ciphertext339);
  evaluator.add_inplace(ciphertext336, ciphertext343);
  evaluator.add_inplace(ciphertext336, ciphertext348);
  evaluator.multiply_plain_inplace(ciphertext336, plaintext19);
  seal::Ciphertext ciphertext358 = ciphertext336;
  std::vector<std::int64_t> plaintext6_clear = {1, -10, 9, 2};
  seal::Plaintext plaintext6;
  encoder.encode(plaintext6_clear, plaintext6);
  seal::Ciphertext ciphertext19;
  encryptor.encrypt(plaintext6, ciphertext19);
  seal::Ciphertext ciphertext20 = ciphertext19;
  seal::Ciphertext ciphertext25 = ciphertext20;
  seal::Ciphertext ciphertext306;
  evaluator.multiply(ciphertext16, ciphertext25, ciphertext306);
  seal::Ciphertext ciphertext307 = ciphertext306;
  seal::Ciphertext ciphertext308 = ciphertext307;
  seal::Ciphertext ciphertext310;
  evaluator.rotate_vector(ciphertext308, 1, galois_keys, ciphertext310);
  seal::Ciphertext ciphertext312 = ciphertext310;
  evaluator.rotate_vector_inplace(ciphertext312, 1, galois_keys);
  seal::Ciphertext ciphertext316 = ciphertext312;
  evaluator.rotate_vector_inplace(ciphertext316, 1, galois_keys);
  seal::Ciphertext ciphertext321 = ciphertext316;
  evaluator.rotate_vector_inplace(ciphertext321, 1, galois_keys);
  seal::Ciphertext ciphertext309 = ciphertext308;
  evaluator.add_inplace(ciphertext309, ciphertext310);
  evaluator.add_inplace(ciphertext309, ciphertext312);
  evaluator.add_inplace(ciphertext309, ciphertext316);
  evaluator.add_inplace(ciphertext309, ciphertext321);
  evaluator.multiply_plain_inplace(ciphertext309, plaintext19);
  seal::Ciphertext ciphertext331 = ciphertext309;
  seal::Ciphertext ciphertext360 = ciphertext331;
  std::vector<std::int64_t> plaintext5_clear = {0, -7, 1, -8};
  seal::Plaintext plaintext5;
  encoder.encode(plaintext5_clear, plaintext5);
  seal::Ciphertext ciphertext17;
  encryptor.encrypt(plaintext5, ciphertext17);
  seal::Ciphertext ciphertext18 = ciphertext17;
  seal::Ciphertext ciphertext21 = ciphertext18;
  seal::Ciphertext ciphertext24 = ciphertext21;
  evaluator.multiply_inplace(ciphertext16, ciphertext24);
  seal::Ciphertext ciphertext282 = ciphertext16;
  seal::Ciphertext ciphertext283 = ciphertext282;
  seal::Ciphertext ciphertext285;
  evaluator.rotate_vector(ciphertext283, 1, galois_keys, ciphertext285);
  seal::Ciphertext ciphertext287 = ciphertext285;
  evaluator.rotate_vector_inplace(ciphertext287, 1, galois_keys);
  seal::Ciphertext ciphertext291 = ciphertext287;
  evaluator.rotate_vector_inplace(ciphertext291, 1, galois_keys);
  seal::Ciphertext ciphertext296 = ciphertext291;
  evaluator.rotate_vector_inplace(ciphertext296, 1, galois_keys);
  seal::Ciphertext ciphertext284 = ciphertext283;
  evaluator.add_inplace(ciphertext284, ciphertext285);
  evaluator.add_inplace(ciphertext284, ciphertext287);
  evaluator.add_inplace(ciphertext284, ciphertext291);
  evaluator.add_inplace(ciphertext284, ciphertext296);
  evaluator.multiply_plain_inplace(ciphertext284, plaintext19);
  seal::Ciphertext ciphertext305 = ciphertext284;
  seal::Ciphertext ciphertext332 = ciphertext305;
  seal::Ciphertext ciphertext359 = ciphertext332;
  seal::Ciphertext ciphertext361 = ciphertext359;
  evaluator.add_inplace(ciphertext361, ciphertext360);
  evaluator.add_inplace(ciphertext361, ciphertext358);
  seal::Ciphertext output3 = ciphertext361;
  seal::Ciphertext ciphertext418;
  evaluator.multiply(ciphertext10, ciphertext23, ciphertext418);
  seal::Ciphertext ciphertext419 = ciphertext418;
  seal::Ciphertext ciphertext420 = ciphertext419;
  seal::Ciphertext ciphertext422;
  evaluator.rotate_vector(ciphertext420, 1, galois_keys, ciphertext422);
  seal::Ciphertext ciphertext424 = ciphertext422;
  evaluator.rotate_vector_inplace(ciphertext424, 1, galois_keys);
  seal::Ciphertext ciphertext428 = ciphertext424;
  evaluator.rotate_vector_inplace(ciphertext428, 1, galois_keys);
  seal::Ciphertext ciphertext433 = ciphertext428;
  evaluator.rotate_vector_inplace(ciphertext433, 1, galois_keys);
  seal::Ciphertext ciphertext421 = ciphertext420;
  evaluator.add_inplace(ciphertext421, ciphertext422);
  evaluator.add_inplace(ciphertext421, ciphertext424);
  evaluator.add_inplace(ciphertext421, ciphertext428);
  evaluator.add_inplace(ciphertext421, ciphertext433);
  evaluator.multiply_plain_inplace(ciphertext421, plaintext19);
  seal::Ciphertext ciphertext443 = ciphertext421;
  seal::Ciphertext ciphertext391;
  evaluator.multiply(ciphertext10, ciphertext25, ciphertext391);
  seal::Ciphertext ciphertext392 = ciphertext391;
  seal::Ciphertext ciphertext393 = ciphertext392;
  seal::Ciphertext ciphertext395;
  evaluator.rotate_vector(ciphertext393, 1, galois_keys, ciphertext395);
  seal::Ciphertext ciphertext397 = ciphertext395;
  evaluator.rotate_vector_inplace(ciphertext397, 1, galois_keys);
  seal::Ciphertext ciphertext401 = ciphertext397;
  evaluator.rotate_vector_inplace(ciphertext401, 1, galois_keys);
  seal::Ciphertext ciphertext406 = ciphertext401;
  evaluator.rotate_vector_inplace(ciphertext406, 1, galois_keys);
  seal::Ciphertext ciphertext394 = ciphertext393;
  evaluator.add_inplace(ciphertext394, ciphertext395);
  evaluator.add_inplace(ciphertext394, ciphertext397);
  evaluator.add_inplace(ciphertext394, ciphertext401);
  evaluator.add_inplace(ciphertext394, ciphertext406);
  evaluator.multiply_plain_inplace(ciphertext394, plaintext19);
  seal::Ciphertext ciphertext416 = ciphertext394;
  seal::Ciphertext ciphertext445 = ciphertext416;
  seal::Ciphertext ciphertext366;
  evaluator.multiply(ciphertext10, ciphertext24, ciphertext366);
  seal::Ciphertext ciphertext367 = ciphertext366;
  seal::Ciphertext ciphertext368 = ciphertext367;
  seal::Ciphertext ciphertext370;
  evaluator.rotate_vector(ciphertext368, 1, galois_keys, ciphertext370);
  seal::Ciphertext ciphertext372 = ciphertext370;
  evaluator.rotate_vector_inplace(ciphertext372, 1, galois_keys);
  seal::Ciphertext ciphertext376 = ciphertext372;
  evaluator.rotate_vector_inplace(ciphertext376, 1, galois_keys);
  seal::Ciphertext ciphertext381 = ciphertext376;
  evaluator.rotate_vector_inplace(ciphertext381, 1, galois_keys);
  seal::Ciphertext ciphertext369 = ciphertext368;
  evaluator.add_inplace(ciphertext369, ciphertext370);
  evaluator.add_inplace(ciphertext369, ciphertext372);
  evaluator.add_inplace(ciphertext369, ciphertext376);
  evaluator.add_inplace(ciphertext369, ciphertext381);
  evaluator.multiply_plain_inplace(ciphertext369, plaintext19);
  seal::Ciphertext ciphertext390 = ciphertext369;
  seal::Ciphertext ciphertext417 = ciphertext390;
  seal::Ciphertext ciphertext444 = ciphertext417;
  seal::Ciphertext ciphertext446 = ciphertext444;
  evaluator.add_inplace(ciphertext446, ciphertext445);
  evaluator.add_inplace(ciphertext446, ciphertext443);
  seal::Ciphertext output4 = ciphertext446;
  std::vector<std::int64_t> plaintext2_clear = {1, 0, 1, -3};
  seal::Plaintext plaintext2;
  encoder.encode(plaintext2_clear, plaintext2);
  seal::Ciphertext ciphertext5;
  encryptor.encrypt(plaintext2, ciphertext5);
  seal::Ciphertext ciphertext6 = ciphertext5;
  seal::Ciphertext ciphertext15 = ciphertext6;
  seal::Ciphertext ciphertext248;
  evaluator.multiply(ciphertext15, ciphertext23, ciphertext248);
  seal::Ciphertext ciphertext249 = ciphertext248;
  seal::Ciphertext ciphertext250 = ciphertext249;
  seal::Ciphertext ciphertext252;
  evaluator.rotate_vector(ciphertext250, 1, galois_keys, ciphertext252);
  seal::Ciphertext ciphertext254 = ciphertext252;
  evaluator.rotate_vector_inplace(ciphertext254, 1, galois_keys);
  seal::Ciphertext ciphertext258 = ciphertext254;
  evaluator.rotate_vector_inplace(ciphertext258, 1, galois_keys);
  seal::Ciphertext ciphertext263 = ciphertext258;
  evaluator.rotate_vector_inplace(ciphertext263, 1, galois_keys);
  seal::Ciphertext ciphertext251 = ciphertext250;
  evaluator.add_inplace(ciphertext251, ciphertext252);
  evaluator.add_inplace(ciphertext251, ciphertext254);
  evaluator.add_inplace(ciphertext251, ciphertext258);
  evaluator.add_inplace(ciphertext251, ciphertext263);
  evaluator.multiply_plain_inplace(ciphertext251, plaintext19);
  seal::Ciphertext ciphertext273 = ciphertext251;
  seal::Ciphertext ciphertext221;
  evaluator.multiply(ciphertext15, ciphertext25, ciphertext221);
  seal::Ciphertext ciphertext222 = ciphertext221;
  seal::Ciphertext ciphertext223 = ciphertext222;
  seal::Ciphertext ciphertext225;
  evaluator.rotate_vector(ciphertext223, 1, galois_keys, ciphertext225);
  seal::Ciphertext ciphertext227 = ciphertext225;
  evaluator.rotate_vector_inplace(ciphertext227, 1, galois_keys);
  seal::Ciphertext ciphertext231 = ciphertext227;
  evaluator.rotate_vector_inplace(ciphertext231, 1, galois_keys);
  seal::Ciphertext ciphertext236 = ciphertext231;
  evaluator.rotate_vector_inplace(ciphertext236, 1, galois_keys);
  seal::Ciphertext ciphertext224 = ciphertext223;
  evaluator.add_inplace(ciphertext224, ciphertext225);
  evaluator.add_inplace(ciphertext224, ciphertext227);
  evaluator.add_inplace(ciphertext224, ciphertext231);
  evaluator.add_inplace(ciphertext224, ciphertext236);
  evaluator.multiply_plain_inplace(ciphertext224, plaintext19);
  seal::Ciphertext ciphertext246 = ciphertext224;
  seal::Ciphertext ciphertext275 = ciphertext246;
  evaluator.multiply_inplace(ciphertext15, ciphertext24);
  seal::Ciphertext ciphertext197 = ciphertext15;
  seal::Ciphertext ciphertext198 = ciphertext197;
  seal::Ciphertext ciphertext200;
  evaluator.rotate_vector(ciphertext198, 1, galois_keys, ciphertext200);
  seal::Ciphertext ciphertext202 = ciphertext200;
  evaluator.rotate_vector_inplace(ciphertext202, 1, galois_keys);
  seal::Ciphertext ciphertext206 = ciphertext202;
  evaluator.rotate_vector_inplace(ciphertext206, 1, galois_keys);
  seal::Ciphertext ciphertext211 = ciphertext206;
  evaluator.rotate_vector_inplace(ciphertext211, 1, galois_keys);
  seal::Ciphertext ciphertext199 = ciphertext198;
  evaluator.add_inplace(ciphertext199, ciphertext200);
  evaluator.add_inplace(ciphertext199, ciphertext202);
  evaluator.add_inplace(ciphertext199, ciphertext206);
  evaluator.add_inplace(ciphertext199, ciphertext211);
  evaluator.multiply_plain_inplace(ciphertext199, plaintext19);
  seal::Ciphertext ciphertext220 = ciphertext199;
  seal::Ciphertext ciphertext247 = ciphertext220;
  seal::Ciphertext ciphertext274 = ciphertext247;
  seal::Ciphertext ciphertext276 = ciphertext274;
  evaluator.add_inplace(ciphertext276, ciphertext275);
  evaluator.add_inplace(ciphertext276, ciphertext273);
  seal::Ciphertext output2 = ciphertext276;
  std::vector<std::int64_t> plaintext1_clear = {-5, 3, 2, 0};
  seal::Plaintext plaintext1;
  encoder.encode(plaintext1_clear, plaintext1);
  seal::Ciphertext ciphertext2;
  encryptor.encrypt(plaintext1, ciphertext2);
  seal::Ciphertext ciphertext3 = ciphertext2;
  seal::Ciphertext ciphertext8 = ciphertext3;
  seal::Ciphertext ciphertext14 = ciphertext8;
  seal::Ciphertext ciphertext163;
  evaluator.multiply(ciphertext23, ciphertext14, ciphertext163);
  seal::Ciphertext ciphertext164 = ciphertext163;
  seal::Ciphertext ciphertext165 = ciphertext164;
  seal::Ciphertext ciphertext167;
  evaluator.rotate_vector(ciphertext165, 1, galois_keys, ciphertext167);
  seal::Ciphertext ciphertext169 = ciphertext167;
  evaluator.rotate_vector_inplace(ciphertext169, 1, galois_keys);
  seal::Ciphertext ciphertext173 = ciphertext169;
  evaluator.rotate_vector_inplace(ciphertext173, 1, galois_keys);
  seal::Ciphertext ciphertext178 = ciphertext173;
  evaluator.rotate_vector_inplace(ciphertext178, 1, galois_keys);
  seal::Ciphertext ciphertext166 = ciphertext165;
  evaluator.add_inplace(ciphertext166, ciphertext167);
  evaluator.add_inplace(ciphertext166, ciphertext169);
  evaluator.add_inplace(ciphertext166, ciphertext173);
  evaluator.add_inplace(ciphertext166, ciphertext178);
  evaluator.multiply_plain_inplace(ciphertext166, plaintext19);
  seal::Ciphertext ciphertext188 = ciphertext166;
  seal::Ciphertext ciphertext136;
  evaluator.multiply(ciphertext14, ciphertext25, ciphertext136);
  seal::Ciphertext ciphertext137 = ciphertext136;
  seal::Ciphertext ciphertext138 = ciphertext137;
  seal::Ciphertext ciphertext140;
  evaluator.rotate_vector(ciphertext138, 1, galois_keys, ciphertext140);
  seal::Ciphertext ciphertext142 = ciphertext140;
  evaluator.rotate_vector_inplace(ciphertext142, 1, galois_keys);
  seal::Ciphertext ciphertext146 = ciphertext142;
  evaluator.rotate_vector_inplace(ciphertext146, 1, galois_keys);
  seal::Ciphertext ciphertext151 = ciphertext146;
  evaluator.rotate_vector_inplace(ciphertext151, 1, galois_keys);
  seal::Ciphertext ciphertext139 = ciphertext138;
  evaluator.add_inplace(ciphertext139, ciphertext140);
  evaluator.add_inplace(ciphertext139, ciphertext142);
  evaluator.add_inplace(ciphertext139, ciphertext146);
  evaluator.add_inplace(ciphertext139, ciphertext151);
  evaluator.multiply_plain_inplace(ciphertext139, plaintext19);
  seal::Ciphertext ciphertext161 = ciphertext139;
  seal::Ciphertext ciphertext190 = ciphertext161;
  evaluator.multiply_inplace(ciphertext14, ciphertext24);
  seal::Ciphertext ciphertext112 = ciphertext14;
  seal::Ciphertext ciphertext113 = ciphertext112;
  seal::Ciphertext ciphertext115;
  evaluator.rotate_vector(ciphertext113, 1, galois_keys, ciphertext115);
  seal::Ciphertext ciphertext117 = ciphertext115;
  evaluator.rotate_vector_inplace(ciphertext117, 1, galois_keys);
  seal::Ciphertext ciphertext121 = ciphertext117;
  evaluator.rotate_vector_inplace(ciphertext121, 1, galois_keys);
  seal::Ciphertext ciphertext126 = ciphertext121;
  evaluator.rotate_vector_inplace(ciphertext126, 1, galois_keys);
  seal::Ciphertext ciphertext114 = ciphertext113;
  evaluator.add_inplace(ciphertext114, ciphertext115);
  evaluator.add_inplace(ciphertext114, ciphertext117);
  evaluator.add_inplace(ciphertext114, ciphertext121);
  evaluator.add_inplace(ciphertext114, ciphertext126);
  evaluator.multiply_plain_inplace(ciphertext114, plaintext19);
  seal::Ciphertext ciphertext135 = ciphertext114;
  seal::Ciphertext ciphertext162 = ciphertext135;
  seal::Ciphertext ciphertext189 = ciphertext162;
  seal::Ciphertext ciphertext191 = ciphertext189;
  evaluator.add_inplace(ciphertext191, ciphertext190);
  evaluator.add_inplace(ciphertext191, ciphertext188);
  seal::Ciphertext output1 = ciphertext191;
  std::vector<std::int64_t> plaintext0_clear = {1, 2, 3, -2};
  seal::Plaintext plaintext0;
  encoder.encode(plaintext0_clear, plaintext0);
  seal::Ciphertext ciphertext0;
  encryptor.encrypt(plaintext0, ciphertext0);
  seal::Ciphertext ciphertext1 = ciphertext0;
  seal::Ciphertext ciphertext4 = ciphertext1;
  seal::Ciphertext ciphertext7 = ciphertext4;
  seal::Ciphertext ciphertext13 = ciphertext7;
  evaluator.multiply_inplace(ciphertext23, ciphertext13);
  seal::Ciphertext ciphertext79 = ciphertext23;
  seal::Ciphertext ciphertext80 = ciphertext79;
  seal::Ciphertext ciphertext82;
  evaluator.rotate_vector(ciphertext80, 1, galois_keys, ciphertext82);
  seal::Ciphertext ciphertext84 = ciphertext82;
  evaluator.rotate_vector_inplace(ciphertext84, 1, galois_keys);
  seal::Ciphertext ciphertext88 = ciphertext84;
  evaluator.rotate_vector_inplace(ciphertext88, 1, galois_keys);
  seal::Ciphertext ciphertext93 = ciphertext88;
  evaluator.rotate_vector_inplace(ciphertext93, 1, galois_keys);
  seal::Ciphertext ciphertext81 = ciphertext80;
  evaluator.add_inplace(ciphertext81, ciphertext82);
  evaluator.add_inplace(ciphertext81, ciphertext84);
  evaluator.add_inplace(ciphertext81, ciphertext88);
  evaluator.add_inplace(ciphertext81, ciphertext93);
  evaluator.multiply_plain_inplace(ciphertext81, plaintext19);
  seal::Ciphertext ciphertext103 = ciphertext81;
  evaluator.multiply_inplace(ciphertext25, ciphertext13);
  seal::Ciphertext ciphertext52 = ciphertext25;
  seal::Ciphertext ciphertext53 = ciphertext52;
  seal::Ciphertext ciphertext55;
  evaluator.rotate_vector(ciphertext53, 1, galois_keys, ciphertext55);
  seal::Ciphertext ciphertext57 = ciphertext55;
  evaluator.rotate_vector_inplace(ciphertext57, 1, galois_keys);
  seal::Ciphertext ciphertext61 = ciphertext57;
  evaluator.rotate_vector_inplace(ciphertext61, 1, galois_keys);
  seal::Ciphertext ciphertext66 = ciphertext61;
  evaluator.rotate_vector_inplace(ciphertext66, 1, galois_keys);
  seal::Ciphertext ciphertext54 = ciphertext53;
  evaluator.add_inplace(ciphertext54, ciphertext55);
  evaluator.add_inplace(ciphertext54, ciphertext57);
  evaluator.add_inplace(ciphertext54, ciphertext61);
  evaluator.add_inplace(ciphertext54, ciphertext66);
  evaluator.multiply_plain_inplace(ciphertext54, plaintext19);
  seal::Ciphertext ciphertext76 = ciphertext54;
  seal::Ciphertext ciphertext105 = ciphertext76;
  evaluator.multiply_inplace(ciphertext13, ciphertext24);
  seal::Ciphertext ciphertext27 = ciphertext13;
  seal::Ciphertext ciphertext28 = ciphertext27;
  seal::Ciphertext ciphertext30;
  evaluator.rotate_vector(ciphertext28, 1, galois_keys, ciphertext30);
  seal::Ciphertext ciphertext32 = ciphertext30;
  evaluator.rotate_vector_inplace(ciphertext32, 1, galois_keys);
  seal::Ciphertext ciphertext36 = ciphertext32;
  evaluator.rotate_vector_inplace(ciphertext36, 1, galois_keys);
  seal::Ciphertext ciphertext41 = ciphertext36;
  evaluator.rotate_vector_inplace(ciphertext41, 1, galois_keys);
  seal::Ciphertext ciphertext29 = ciphertext28;
  evaluator.add_inplace(ciphertext29, ciphertext30);
  evaluator.add_inplace(ciphertext29, ciphertext32);
  evaluator.add_inplace(ciphertext29, ciphertext36);
  evaluator.add_inplace(ciphertext29, ciphertext41);
  evaluator.multiply_plain_inplace(ciphertext29, plaintext19);
  seal::Ciphertext ciphertext50 = ciphertext29;
  seal::Ciphertext ciphertext77 = ciphertext50;
  seal::Ciphertext ciphertext104 = ciphertext77;
  seal::Ciphertext ciphertext106 = ciphertext104;
  evaluator.add_inplace(ciphertext106, ciphertext105);
  evaluator.add_inplace(ciphertext106, ciphertext103);
  seal::Ciphertext output0 = ciphertext106;
  encrypted_outputs.insert({"output3", output3});
  encrypted_outputs.insert({"output4", output4});
  encrypted_outputs.insert({"output2", output2});
  encrypted_outputs.insert({"output1", output1});
  encrypted_outputs.insert({"output0", output0});
}
